# 文案怎麼改

**一句話：改 Sheet，跑一支腳本寫回去。你不用碰 git,也不用開程式碼。**

```
recipes.js + index.html          ← app 真正讀的，唯一真相
   │
   │  python gen_locale.py
   ↓
語系總表.md（給人讀）+ locale.tsv（給 Sheet 吃）
   │
   │  Sheet 選單「慢寶 → 從 GitHub 抓最新文案」
   ↓
Google Sheet ── 你在任何一台電腦、甚至手機上改 ──┐
   │                                            │
   │  檔案 → 下載 → csv                          │
   ↓                                            │
python apply_locale.py 下載的檔案.csv --write ───┘
   ↓
寫回 recipes.js + index.html
```

---

## 第一次要做的設定（只做一次）

1. 開一張新的 Google Sheet,取名「慢寶 · 文案」
2. **擴充功能 → Apps Script**
3. 把 `bo_locale_sheet.gs` 的內容整份貼進去,存檔
4. 回到試算表重新整理,上方會出現「慢寶」選單
5. 點 **慢寶 → 從 GitHub 抓最新文案**

第一次執行 Google 會問要不要授權(它要連外抓檔案),按同意。
**不需要任何 token 或密碼** —— repo 是公開的,它只是去讀一個網址。

---

## 每次審稿

**開始前一定先「從 GitHub 抓最新文案」。** 這張表是工作檯,不是真相 ——
上次留在上面的可能已經舊了。2026-08-16 踩過一次:一份手寫的表跟 app 差了 8 處,
全部是「表是舊快照」。

改完 → **檔案 → 下載 → 逗號分隔值(.csv)** → 把檔案給我,或自己跑:

```bash
python apply_locale.py 下載的檔案.csv
```

**預設只預演,印出會改哪幾句,不動任何檔案。** 確認沒問題再加 `--write`:

```bash
python apply_locale.py 下載的檔案.csv --write
```

寫完再跑一次 `python gen_locale.py`,總表和 locale.tsv 就跟著更新。

---

## 三條規矩

**⚠️ 不要改 ID 欄。** 那是寫回程式碼的定位依據,改了就對不上,那一句會被跳過。

**⚠️ TERM／COUNTER／NOTE 只能改韓文。** 這三類的中文本身就是編號(`TERM.洋蔥`),
改中文等於改編號。腳本會擋下來並告訴你。

**⚠️ 位置欄(灰色的)只是告訴你這句住在哪,不用管它。**

「備註」欄是你的,愛寫什麼寫什麼 —— 腳本完全不看它,而且重新抓資料時會依編號保留。

---

## 出事的時候

腳本有三道保險:

1. **預設不寫**,要 `--write` 才動檔案
2. **逐句核對**:每一句都比對「Sheet 上的舊值」跟「程式碼現在的值」。
   對不上就跳過並報出來 —— 代表 Sheet 是舊的,或程式碼被改過
3. **index.html 是外科式替換**,不重新格式化 ——
   那份檔案裡的註解(每條 COUNTER_OVERRIDES 為什麼存在)比程式碼還值錢

真的寫壞了,`git checkout -- index.html recipes.js` 就全部退回去。

---

## 檔案分工

| 檔案 | 做什麼 |
|---|---|
| `locale_lib.py` | 底層。認得出每一句在哪裡,兩支腳本共用 |
| `gen_locale.py` | 匯出:程式碼 → `語系總表.md` ＋ `locale.tsv` |
| `apply_locale.py` | 寫回:Sheet 匯出的 csv → 程式碼 |
| `bo_locale_sheet.gs` | 貼進 Google Sheet 的 Apps Script |

⚠️ **`語系總表.md` 和 `locale.tsv` 都是生成物,不要手改** —— 下次重跑就沒了。
