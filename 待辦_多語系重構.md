# 待辦：多語系重構（加英文之前要做）

**狀態：未開始。** 2026-08-18 討論並實測過範圍，決定等有完整時段再動。

**不急。** 中韓兩語現在有六個啟動稽核守著（見底下〈為什麼可以等〉）。
真正會失控是**第三種語言進來的時候**。

---

## 要解決什麼

現在的中韓對照散在五個地方，**每個地方的「找法」都不一樣**：

| 在哪 | 怎麼找 | 風險 |
|---|---|---|
| `T`（介面＋固定台詞，約 50 筆） | `KEY: ['中', '한']` 靠陣列位置 | 加語言＝每筆多一格 |
| `TERM`（詞彙 167 筆） | `'中': '한'` —— **中文當 key** | ⚠️ 改中文，韓文立刻變孤兒 |
| `NOTE`（括號註 33 筆） | 同上 | ⚠️ 同上 |
| `COUNTER_OVERRIDES`（量詞 8 筆） | `'食材\|單位': '한'` | 複合 key，加語言更麻煩 |
| `recipes.js` 的 `r.ko` | `ko.steps[i]` 對第 i 句 —— **靠位置** | ⛔ 插一句就整段錯位，**不會報錯** |
| `BO_INTRO_LINES` / `BO_INTRO_KO` | 兩個各自獨立的陣列 | 連表都不是 |

**兩個已經實際踩過的：**

1. **中文當 key** —— 2026-08-18 改括號註兩次，兩次都要手動同步 `NOTE` 表，
   否則韓文那筆對不到任何東西（`auditNotes()` 會喊，但那是事後）
2. **位置對位** —— 同日 R14 加「蝦頭煎香」那步時，中文 `steps` 插一句，
   `ko.steps` 必須在**同一個位置**插一句。靠腳本裡的 assert 才擋下來

---

## 目標形狀

**全部改成「編號當 key、語言當欄位」。**

```js
const STR = {
  'U8':        { zh: '食譜牆',  ko: '레시피',  en: 'Recipes' },
  'R5.note':   { zh: '…',      ko: '…',      en: '…' },
  'TERM.洋蔥':  { zh: '洋蔥',   ko: '양파',    en: 'onion' },
};
```

三個好處：

- **一種形狀** —— 加語言＝加一個欄位，不是五種不同的改法
- **中文不再是 key** —— 改中文不會斷任何連結
- **不再靠位置對位** —— 每一句有自己的編號，插隊也不影響

📌 **編號規則已經存在，不用發明。** 就是 `語系總表.md` 裡在用的那套
（`R5.note`、`R5.step.3`、`U15`、`TERM.洋蔥`、`COUNTER.大蔥|根`、`NOTE.關火前`）。
`gen_locale.py` 已經在生它們，`apply_locale.py` 已經靠它們定位。
**工作不是發明編號，是把程式碼也改成用編號找。**

---

## 實測的範圍（2026-08-18 量的）

### 好消息：呼叫點不用改

app 早就有四個存取器把查表隔開了：

```
t()   91 個呼叫點  →  只讀 T
tr()   7 個        →  只讀 TERM
L()   12 個        →  只讀 r.ko[欄位]
La()   4 個        →  只讀 r.ko[欄位][index]
```

**114 個呼叫點全部走這四個函式。** 直接讀表的只有 18 處：
`TERM[` 4、`NOTE[` 3、`COUNTER_OVERRIDES[` 3、`BO_INTRO` 8。

所以程式面要動的是 **4 個函式的內部 ＋ 18 處直接存取**，不是 114 個地方。

### 工作量主要在資料轉換

| 要轉的 | 筆數 |
|---|---|
| `T` | 約 50 |
| `TERM` | 167 |
| `NOTE` | 33 |
| `COUNTER_OVERRIDES` | 8 |
| `BO_INTRO_LINES` / `_KO` | 6 × 2 |
| `recipes.js` 的 `ko` 覆蓋層 | 17 道 × 8 欄位，**339 個陣列元素** |

這些可以寫腳本轉，但**轉完要逐一驗**。

### 三支 Python 也要跟著改

`locale_lib.py`（抽取）、`gen_locale.py`（匯出）、`apply_locale.py`（寫回）——
它們現在為五種形狀各寫了一條路徑，改完只需要一條。

**這是重構的附加好處，不是額外成本。**

### 估計

**2–3 小時，含驗證。**

⚠️ 2026-08-18 我第一次口頭估「半天」，那是**沒量就講的**。量完之後降到 2–3 小時，
因為存取器已經做掉大半隔離工作。

---

## 為什麼可以等

六個稽核在開 app 時自動跑，**三個結構風險裡有兩個已經被擋住**：

| 稽核 | 擋什麼 | 等級 |
|---|---|---|
| `auditAlignment()` | **中韓陣列錯位** —— 就是上面那個「不會報錯」的 | ⛔ error |
| `auditNotes()` | 括號註缺韓文（改中文造成孤兒時會現形） | ⚠️ warn |
| `auditSubtitles()` | 副標會折行 | ⚠️ warn |
| `auditCounters()` | 量詞沒登記 | ⚠️ warn |
| `auditUnits()` | 單位缺韓文 | ⚠️ warn |
| `auditKorean()` | 韓文用語要人工確認 | ⚠️ warn |

剩下沒被擋的是「加語言要改五個地方」—— 而那個**只有真的要加英文時才痛**。

---

## ⚠️ 開工前先確認

- **食譜的非文字部分不能進 `STR`** —— `id`、`basePortions`、`image`、`shoppingList`
  的數字、`stars`、`time`。那些不是文案，是資料
- **`author: "Stephany"` 這個欄位是死的**（17 道都有，`index.html` 從沒讀過它）。
  重構時順手決定要刪還是留
- **做之前先確認文案翻修告一段落** —— 重構期間文案不好改，兩件事撞在一起會很痛苦
- **不要半途留過夜** —— 中途的話 app 是壞的

---

## 相關檔案

```
index.html          T / TERM / NOTE / COUNTER_OVERRIDES / BO_INTRO_* / 四個存取器 / 六個稽核
recipes.js          每道菜的 ko 覆蓋層
locale_lib.py       抽取，兩支腳本共用
gen_locale.py       匯出 → 語系總表.md ＋ locale.tsv
apply_locale.py     寫回 ← Sheet 匯出的 csv
語系總表.md          編號規則寫在它的開頭
文案怎麼改.md         Sheet 來回的流程
```
