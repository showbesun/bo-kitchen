# 換一台電腦要做什麼

**這份放在公開 repo，所以只寫「怎麼請 Claude 做」，不寫任何私人內容。**

---

## 這台電腦是新的／很久沒開

把下面整段貼給 Claude Code 就好。**你不用上 GitHub，也不用自己記步驟。**

```
幫我把這台電腦接上我的私人記憶 repo，GitHub 上叫 stephany-notes。

⚠️ 這是合併不是下載 —— 這台可能已經有舊版的 project_manbao.md 和 manbao 技能，
   直接 checkout 會蓋掉。

repo 裡有 SETUP-mac.md，寫了完整步驟、佈局為什麼那樣、以及驗證方式。
先 fetch、把那份讀出來，照著做。不要跳過裡面的合併警告。

弄完之後告訴我：技能有沒有載入、兩個共享 repo 有沒有看到私人檔案。
```

### 為什麼這樣就夠

`SETUP-mac.md` 在那個 repo 裡。**`git fetch` 之後、還沒 checkout 就讀得到**：

```bash
git --git-dir=~/.claude/personal.git show origin/main:SETUP-mac.md
```

所以 Claude 可以自己把說明書撈出來看，不用你轉述。
說明書跟著 repo 走，永遠是最新的 —— 比把步驟抄在這裡可靠。

---

## 順序：先接記憶，再拉程式碼

```bash
cd ~/Projects/bo-kitchen && git pull
```

**先接記憶** —— 否則 Claude 讀到的是舊的專案事實（會以為只有 12 道菜、
不知道有文案規範），然後很有自信地講錯的事。那比沒有記憶更糟。

---

## 四個 repo，各管各的

| repo | 可見度 | 裝什麼 |
|---|---|---|
| `bo-kitchen` | 公開 | 程式碼、資料、這類說明。**線上版從它出來，所以必須公開** |
| `stephany-notes` | 私有 | 記憶、知識庫地圖、`manbao` 技能 |
| `wyd-memory` | 共享 | 工作記憶（同事讀得到） |
| `wyd-skills` | 共享 | 工作技能 |

前兩個是個人的，後兩個是工作的、跟同事共用。

⚠️ **放進 `bo-kitchen` 就等於公開發布。** 開發筆記、決策過程、對話原話
一律留在 `stephany-notes`。判準：**打開 repo 的陌生人需要它嗎？**
