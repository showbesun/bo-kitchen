# 跟煮模式 —— ⛔ 已放棄（2026-08-19）

**不要重做。** 設計做完了、演算法做完了、可點的草圖也做完了，
在她實際滑過之後決定不要。原因不是技術問題：

> 每個人的速度都不一樣，做法也不一樣，實在不用硬性規定哪些步驟要一起做。
> 架設的過程太痛苦，用起來似乎也沒想像順手。

⭐ **這是產品判斷，不是「還沒做好」。** 把三道菜的步驟合併成一條時間軸，
本質上是在假設所有人的節奏一樣 —— 而下廚正好是節奏因人而異的事。
做得再準也解不掉那個前提。

---

## 做過什麼（都已從 repo 移除，git 歷史裡還有）

| | |
|---|---|
| `_proto3.py` | 分頁演算法。1330 組（21 道取 3）壓力測試零例外，平均 7.4 頁（一步一頁是 15.9 頁） |
| `_cook_mock.html` | 可點的草圖，跑真資料 |
| `schedule` 欄位 | 21 道各 5–8 列的 `[分鐘, 在做什麼]`。**一併刪除**，見下 |
| `schedule_時間表.md` | 給她編輯 schedule 的表 |

## 為什麼連 `schedule` 一起刪

它從來沒有被 `index.html` 讀過，而且**跟步驟文字已經互相矛盾**：

```
r2 海帶湯   schedule：轉中小火慢燉 15 分鐘     步驟：蓋上鍋蓋燉 30 分鐘
            schedule：維持中大火滾煮 12 分鐘   步驟：滾煮 10 分鐘
```

108 條「開火後」的 schedule 列裡，只有 1 條的文字跟某個步驟幾乎相同、
63 條完全對不上 —— 它是**另外寫的一份摘要**，不是步驟的時間標註。
留著一份沒人讀、又跟正確資料打架的東西，下次有人拿它去做什麼就會踩到。

## 這一輪留下來的（跟跟煮模式無關，本身就是對的）

- **櫛瓜統一**：`ING.韓國櫛瓜` 併進 `ING.櫛瓜`（韓文都是 애호박）。
  兩道一起煮時買菜清單不會再列兩行
- **r12 的「倒掉上層水」從備料搬到步驟** —— 那要等澱粉沉澱 10 分鐘，
  備料時根本還沒到那一步
- **r9 / r12 的 `prepTime` 修正**（14→19、10→22）。
  `prepTime` 的意思是「開火從第幾分鐘開始」，填錯會讓卡片上的
  「備料 X 分・開火 Y 分」是錯的
- ⚠️ **r11 銅盤烤肉維持原值**：它的 `prepTime+cookTime` 只有 35 分，
  而醃肉要一小時 —— 那 35 分是「實際要動手的時間」，是刻意的

## 如果哪天又想做

先回答這個問題再動手：**要怎麼處理「每個人速度不一樣」？**
沒有答案就會再走一次同樣的路。
（當時想過但沒解的：按「下一步」推進而不看時鐘 —— 那解決了計時器說謊的問題，
但沒解決「把兩件事併在一頁＝叫你一起做」的問題。）
